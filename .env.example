# Server (see server/.env.example for the copy the server actually reads)
PORT=4000
MONGODB_URI=mongodb://localhost:27017/fengshui-checker
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
UPLOAD_DIR=./uploads

# App (see app/.env.example)
EXPO_PUBLIC_API_URL=http://localhost:4000
